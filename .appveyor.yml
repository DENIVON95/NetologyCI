image:
  - Ubuntu

stack: jdk 11

branches:
  only:
    - master

build: off

install:
  - java -jar ./artifacts/app-mbank.jar &

build_script:
  - gradle wrapper --gradle-version 6.0
  - gradle --version